"use client"; import { FormEvent, useMemo, useState } from "react"; import { useParams } from "next/navigation"; import { Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts"; import { authClient } from "../../auth-client"; import { PageHead, Shell } from "../../components";
const history=[{month:"Feb",rating:1200},{month:"Mar",rating:1265},{month:"Apr",rating:1327},{month:"May",rating:1391},{month:"Jun",rating:1424},{month:"Jul",rating:1462}];export default function Profile(){const {handle}=useParams<{handle:string}>();const {data:session}=authClient.useSession();const [editing,setEditing]=useState(false);const [name,setName]=useState(session?.user.name||"");const [message,setMessage]=useState("");const player=useMemo(()=>handle||"competitor",[handle]);async function save(e:FormEvent){e.preventDefault();const {error}=await authClient.updateUser({name});setMessage(error?.message||"Profile updated.");if(!error)setEditing(false)}return <Shell><main className="page-wrap"><PageHead kicker="PLAYER PROFILE" title={`@${player}`} text="A live record of arena performance, form, and rating growth."/>{session&&<div className="profile-actions"><button className="button button-small" onClick={()=>setEditing(!editing)}>Edit profile</button>{editing&&<form onSubmit={save}><input value={name} onChange={e=>setName(e.target.value)} placeholder="Display name"/><button className="button button-small">Save</button>{message&&<small>{message}</small>}</form>}</div>}<div className="profile-stats"><div><small>CURRENT RATING</small><b>1462</b><em>+38 this month</em></div><div><small>WIN / LOSS</small><b>18 <i>—</i> 14</b><em>56% win rate</em></div><div><small>ACTIVE SINCE</small><b>FEB 2026</b><em>32 rated matches</em></div></div><section className="chart-card"><div><p className="eyebrow">RATING HISTORY</p><h2>Steady progress.</h2></div><div className="rating-chart"><ResponsiveContainer width="100%" height={250}><LineChart data={history}><XAxis dataKey="month" stroke="#606963" tickLine={false}/><YAxis domain={[1150,1500]} stroke="#606963" tickLine={false}/><Tooltip contentStyle={{background:"#141716",border:"1px solid #292e2a"}}/><Line type="monotone" dataKey="rating" stroke="#51e6d5" strokeWidth={3} dot={{fill:"#51e6d5",r:4}}/></LineChart></ResponsiveContainer></div></section></main></Shell>}
