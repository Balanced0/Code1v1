"use client"; import { FormEvent, useState } from "react"; import { PageHead, Shell } from "../components"; import { apiUrl } from "../data";
export default function Contact(){const [message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));try{const r=await fetch(`${apiUrl}/contact`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const d=await r.json();if(!r.ok)throw Error(d.message);setMessage(d.message);e.currentTarget.reset()}catch(error){setMessage(error instanceof Error?error.message:"Could not send your message.")}}return <Shell><main className="page-wrap"><PageHead kicker="GET IN TOUCH" title="A good arena listens." text="Questions, partnerships, or feedback about a matchup? Send us a note."/><form className="contact-form" onSubmit={submit}><label>Name<input required name="name" /></label><label>Email<input required name="email" type="email" /></label><label>Message<textarea required name="message" rows={6}/></label><button className="button">Send message <span>→</span></button>{message&&<p className="form-message">{message}</p>}</form></main></Shell>}
