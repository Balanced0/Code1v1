"use client"; import { useState } from "react"; import Editor from "@monaco-editor/react"; import { useParams } from "next/navigation"; import { Shell } from "../../components"; import { problems } from "../../data";
export default function Match(){const {id}=useParams<{id:string}>();const [status,setStatus]=useState("Ready");const [language,setLanguage]=useState("cpp");const problem=problems[3];return <Shell><main className="match-page"><header><div><p className="eyebrow">MATCH #{id.slice(0,8).toUpperCase()}</p><h1>{problem.title}</h1></div><div className="match-timer"><small>TIME ELAPSED</small><b>00:00</b></div><div className="opponent"><span className="avatar lime">MN</span><div><small>OPPONENT</small><b>mira_nova · <em>{status}</em></b></div></div></header><div className="match-grid"><article className="match-statement"><p>{problem.description}</p><h2>Constraints</h2><p>{problem.constraints}</p><h2>Sample</h2><pre>{problem.sampleInput}</pre></article><section className="editor-panel"><div className="editor-tools"><select value={language} onChange={e=>setLanguage(e.target.value)}><option value="cpp">C++17</option><option value="python">Python 3</option><option value="javascript">JavaScript</option></select><span>{status}</span></div><Editor height="420px" theme="vs-dark" language={language} defaultValue={language==="python"?"def solve():\n    pass":"#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n  return 0;\n}"} options={{minimap:{enabled:false},fontSize:14,padding:{top:16}}}/><div className="editor-actions"><button onClick={()=>setStatus("Running")}>Run code</button><button className="button" onClick={()=>setStatus("Accepted")}>Submit <span>→</span></button></div></section></div></main></Shell>}
