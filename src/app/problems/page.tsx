"use client";
import Link from "next/link"; import { useMemo, useState } from "react"; import { Difficulty, PageHead, Shell } from "../components"; import { problems } from "../data";
export default function ProblemsPage(){const [query,setQuery]=useState("");const [difficulty,setDifficulty]=useState("All");const [tag,setTag]=useState("All");const [sort,setSort]=useState("newest");const filtered=useMemo(()=>problems.filter(p=>(difficulty==="All"||p.difficulty===difficulty)&&(tag==="All"||p.tags.includes(tag))&&p.title.toLowerCase().includes(query.toLowerCase())).sort((a,b)=>sort==="acceptance"?b.acceptance-a.acceptance:sort==="difficulty"?a.difficulty.localeCompare(b.difficulty):0),[query,difficulty,tag,sort]);return <Shell><div className="page-wrap"><PageHead kicker="PROBLEM VAULT" title="Find your next edge." text="Curated competitive programming problems, arranged for deliberate practice."/><div className="filters"><input aria-label="Search problems" placeholder="Search by title…" value={query} onChange={e=>setQuery(e.target.value)}/><select value={difficulty} onChange={e=>setDifficulty(e.target.value)}><option>All</option><option>Easy</option><option>Medium</option><option>Hard</option></select><select value={tag} onChange={e=>setTag(e.target.value)}><option>All</option><option>implementation</option><option>math</option><option>graphs</option><option>strings</option></select><select value={sort} onChange={e=>setSort(e.target.value)}><option value="newest">Newest</option><option value="difficulty">Difficulty</option><option value="acceptance">Acceptance</option></select></div><div className="problem-grid">{filtered.map((p,index)=><article className="problem-card" key={p.id}><div className="problem-icon">{String(index+1).padStart(2,"0")}</div><div className="card-top"><Difficulty value={p.difficulty}/><span>{p.acceptance}% accepted</span></div><h2>{p.title}</h2><p>{p.description}</p><div className="tags">{p.tags.map(t=><span key={t}>{t}</span>)}</div><Link href={`/problems/${p.id}`} className="card-link">Open problem <span>→</span></Link></article>)}</div>{!filtered.length&&<p className="empty-state">No problems match those filters. Try a broader search.</p>}</div></Shell>}
