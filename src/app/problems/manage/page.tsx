"use client"; import Link from "next/link"; import { useState } from "react"; import { Shell } from "../../components"; import { problems } from "../../data";
export default function Manage(){const [items,setItems]=useState(problems.slice(0,3));return <Shell><main className="page-wrap"><div className="manage-head"><div><p className="eyebrow">YOUR CONTRIBUTIONS</p><h1>Manage problems.</h1></div><Link className="button" href="/problems/add">Add problem <span>+</span></Link></div><div className="manage-list">{items.map(p=><div key={p.id}><b>{p.title}</b><span>{p.difficulty}</span><Link href={`/problems/${p.id}`}>View</Link><button onClick={()=>setItems(items.filter(x=>x.id!==p.id))}>Delete</button></div>)}</div>{!items.length&&<p className="empty-state">No problems remain in this view.</p>}</main></Shell>}
