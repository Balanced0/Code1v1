import Link from "next/link"; import { PageHead, Shell } from "../components";
const players=[["byteblitz",2184,84,22],["sana.codes",2117,72,25],["octal_fox",2063,61,21],["mira.dev",1998,55,28],["demo",1462,18,14]];
export default function Leaderboard(){return <Shell><main className="page-wrap"><PageHead kicker="SEASON 01 · LIVE RANKINGS" title="The board never sleeps." text="Win the match. Take the rating. Your next opponent is waiting."/><div className="leader-table"><div className="leader-row heading"><span>Rank</span><span>Player</span><span>Rating</span><span>Wins</span><span>Win rate</span></div>{players.map(([handle,rating,wins,losses],i)=><Link href={`/profile/${handle}`} className="leader-row" key={handle as string}><b>0{i+1}</b><strong><i>{(handle as string)[0].toUpperCase()}</i>{handle}</strong><em>{rating}</em><span>{wins}</span><span>{Math.round((wins as number)/((wins as number)+(losses as number))*100)}%</span></Link>)}</div></main></Shell>}
