"use client"; import Link from "next/link"; import { FormEvent, useState } from "react"; import { Shell } from "../components"; import { apiUrl } from "../data";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();try{const r=await fetch(`${apiUrl}/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)throw Error(d.message);localStorage.setItem("code1v1-token",d.token);setMessage("Signed in. Your arena session is ready.");}catch(error){setMessage(error instanceof Error?error.message:"Could not sign in.")}}return <Shell><main className="auth-layout"><form onSubmit={submit} className="auth-card"><p className="eyebrow">WELCOME BACK</p><h1>Enter the arena.</h1><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} /></label><label>Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} /></label><button className="button">Sign in <span>→</span></button><button type="button" className="demo-button" onClick={()=>{setEmail("demo@code1v1.dev");setPassword("demo12345")}}>Use demo account</button>{message&&<p className="form-message">{message}</p>}<p>New here? <Link href="/register">Create an account</Link></p></form></main></Shell>}
