"use client"; import Link from "next/link"; import { FormEvent, useState } from "react"; import { Shell } from "../components"; import { apiUrl } from "../data";
export default function Register(){const [form,setForm]=useState({handle:"",email:"",password:""});const [message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();try{const r=await fetch(`${apiUrl}/auth/register`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)throw Error(d.message);localStorage.setItem("code1v1-token",d.token);setMessage("Account created. Welcome to Code1v1.");}catch(error){setMessage(error instanceof Error?error.message:"Could not create account.")}}return <Shell><main className="auth-layout"><form onSubmit={submit} className="auth-card"><p className="eyebrow">JOIN THE LADDER</p><h1>Make your name.</h1>{(["handle","email","password"] as const).map(key=><label key={key}>{key}<input required type={key==="password"?"password":key==="email"?"email":"text"} minLength={key==="password"?8:undefined} value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})}/></label>)}<button className="button">Create account <span>→</span></button>{message&&<p className="form-message">{message}</p>}<p>Already competing? <Link href="/login">Sign in</Link></p></form></main></Shell>}
